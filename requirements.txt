pydantic>=2.5
PyMuPDF>=1.23
python-docx>=1.1
pytesseract>=0.3.10
Pillow>=10.0
requests>=2.31

# Contrôle qualité déclaratif du jeu de données (CdC carte 02 : « Great
# Expectations, exécuté dans la CI — un jeu non conforme bloque la chaîne »).
# pandas est la dépendance de calcul de GE ; elle est déclarée explicitement
# parce que src/m1_ingestion/expectations.py l'importe directement.
great-expectations>=1.0
pandas>=2.0

# Tests (exécutés en CI : `python -m pytest tests/`)
pytest>=8.0

# Orchestration / versioning (only needed to run dags/legal_ingest_v2.py
# and `dvc repro` — not required for `python -m src.m1_ingestion.*` CLIs)
# apache-airflow>=2.8
# dvc>=3.0
